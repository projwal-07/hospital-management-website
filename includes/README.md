# includes/

Shared PHP building blocks reused across pages.

- `header.php`, `nav.php`, `footer.php` — shared page layout (Phase 7)
- helper files for authentication, CSRF tokens and server-side
  validation (Phases 3 and 6)

Direct browser access to this folder is blocked by `.htaccess`.
