# database/

Database definition and documentation.

- `schema.sql` — table structure, data types and relationships (Phase 1)
- `seed.sql` — sample departments and demo accounts (Phase 1)
- `ER-diagram.png` — entity-relationship diagram

Import instructions are added together with the schema in Phase 1.
Direct browser access to this folder is blocked by `.htaccess`.
