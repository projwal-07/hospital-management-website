# config/

Database connection and application configuration.

- `db.example.php` — template with placeholder values (committed to Git)
- `db.php` — the real PDO connection with local credentials
  (created in Phase 2; ignored by Git)

Direct browser access to this folder is blocked by `.htaccess`.
